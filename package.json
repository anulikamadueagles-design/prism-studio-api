{
  "name": "prism-studio-api",
  "version": "1.0.0",
  "description": "Shared AI backend for Prism Studio (Element Tech) — proxies Gemini vision calls so no visitor needs their own API key.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
